# Environment Configuration for Praxify CFO - AIML Engine
# Copy this file to .env and update the values as needed

# Application Environment
ENV=production  # Options: development, staging, production

# API Configuration
API_PORT=8000
LOG_LEVEL=info  # Options: debug, info, warning, error, critical

# CORS Configuration (Frontend domains allowed to access the API)
# For local development: http://localhost:3000,http://127.0.0.1:3000
# For Vercel: Add your Vercel domain, e.g., https://your-app.vercel.app
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://praxify-cfo.vercel.app

# Redis Configuration (for conversational memory)
REDIS_HOST=redis
REDIS_PORT=6379

# Google Gemini API Key (Required for AI features)
# Get your key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=YOUR_GOOGLE_GEMINI_API_KEY_HERE

# Nginx Configuration (only used when starting with nginx profile)
NGINX_PORT=80
NGINX_SSL_PORT=443

# Resource Limits (for docker-compose)
CPU_LIMIT=2
MEMORY_LIMIT=4G

# Optional: External Database Configuration (if needed in future)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Optional: Cloud Storage Configuration (if needed)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=your-bucket-name

# Optional: Monitoring and Logging (if needed)
# SENTRY_DSN=your_sentry_dsn
# LOG_FILE_PATH=/app/logs/app.log
