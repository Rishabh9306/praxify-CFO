# 🏗️ Praxifi Production Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        INTERNET                                  │
│                  (Your Users & Browsers)                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
                  ┌──────────────────┐
                  │   DNS (Namecheap)│
                  │  yourdomain.com  │
                  │  123.45.67.89    │
                  └────────┬─────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
  yourdomain.com    www.yourdomain.com  api.yourdomain.com
        │                  │                  │
        └──────────────────┴──────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                    VPS SERVER (Ubuntu 22.04)                      │
│                        123.45.67.89                               │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                     NGINX (Port 80/443)                  │    │
│  │              SSL/TLS Termination (Let's Encrypt)         │    │
│  │                Rate Limiting & Load Balancing            │    │
│  └──────────────────────┬──────────────────┬────────────────┘    │
│                         │                  │                     │
│          ┌──────────────┴──────┐     ┌─────┴──────────┐         │
│          │                     │     │                │         │
│          ▼                     │     ▼                │         │
│  ┌──────────────┐             │  ┌──────────────┐    │         │
│  │   Frontend   │             │  │   Backend    │    │         │
│  │   Next.js    │             │  │   FastAPI    │    │         │
│  │   (PM2)      │             │  │   (Docker)   │    │         │
│  │   Port 3000  │             │  │   Port 8000  │    │         │
│  │              │             │  │              │    │         │
│  │ • Cluster: 2 │             │  │ • AI/ML      │    │         │
│  │ • Auto-      │             │  │ • Prophet    │    │         │
│  │   restart    │             │  │ • Security   │    │         │
│  │ • Load       │             │  │ • SSE        │    │         │
│  │   balanced   │             │  │ • Privacy    │    │         │
│  └──────────────┘             │  └──────┬───────┘    │         │
│                               │         │            │         │
│                               │         ▼            │         │
│                               │  ┌──────────────┐    │         │
│                               │  │    Redis     │    │         │
│                               │  │   (Docker)   │    │         │
│                               │  │   Port 6379  │    │         │
│                               │  │              │    │         │
│                               │  │ • Convers.   │    │         │
│                               │  │   Memory     │    │         │
│                               │  │ • Cache      │    │         │
│                               │  └──────────────┘    │         │
│                               │                      │         │
│  ┌────────────────────────────┴──────────────────────┴──────┐  │
│  │                  Docker Network (praxifi-network)        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    Docker Volumes                          │ │
│  │  • redis_data     - Redis persistence                     │ │
│  │  • uploads_data   - User-uploaded CSVs                    │ │
│  │  • outputs_data   - Generated reports                     │ │
│  │  • logs_data      - Application logs                      │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                   Filesystem Backups                       │ │
│  │  Location: /opt/praxifi/backups/                          │ │
│  │  Schedule: Daily @ 2 AM (cron)                            │ │
│  │  Retention: 7 days                                        │ │
│  └───────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
```

---

## Traffic Flow

### 1. Frontend Request (https://yourdomain.com)
```
User Browser
    ↓
DNS Resolution (yourdomain.com → 123.45.67.89)
    ↓
HTTPS (Port 443) → Nginx
    ↓
Reverse Proxy to localhost:3000
    ↓
Next.js Frontend (PM2 - Instance 1 or 2)
    ↓
HTML/CSS/JS Served to Browser
```

### 2. API Request (https://api.yourdomain.com/api/full_report)
```
Browser / Frontend
    ↓
HTTPS (Port 443) → Nginx
    ↓
Reverse Proxy to aiml-engine:8000
    ↓
FastAPI Backend (Docker Container)
    ↓
┌─────────────────────────────────────┐
│ 1. File Upload Processing          │
│ 2. Data Validation & Cleaning      │
│ 3. Feature Engineering             │
│ 4. Parallel Forecasting (Prophet)  │
│ 5. Anomaly Detection               │
│ 6. Correlation Analysis            │
│ 7. Visualization Generation        │
│ 8. AI Narrative (Google API)       │
└─────────────────────────────────────┘
    ↓
Redis (Conversational Memory)
    ↓
JSON Response to Frontend
```

### 3. Progress Updates (SSE)
```
Frontend opens EventSource
    ↓
GET https://api.yourdomain.com/api/progress/{task_id}
    ↓
Nginx (SSE-specific config: no buffering)
    ↓
FastAPI Backend (SSE Endpoint)
    ↓
Real-time progress events streamed
    ↓
Frontend updates progress bar (5% → 100%)
```

---

## Port Mapping

| Service | Internal Port | External Port | Protocol |
|---------|---------------|---------------|----------|
| Nginx HTTP | 80 | 80 | HTTP → HTTPS redirect |
| Nginx HTTPS | 443 | 443 | HTTPS (SSL/TLS) |
| FastAPI Backend | 8000 | 8000* | HTTP (internal) |
| Next.js Frontend | 3000 | 3000* | HTTP (internal) |
| Redis | 6379 | 6380* | TCP (internal) |

*Ports marked with asterisk are only accessible internally via Nginx reverse proxy or localhost.

---

## Resource Allocation

### VPS Total Resources
- **CPU**: 4 vCPUs (or 8 recommended)
- **RAM**: 8GB (or 16GB recommended)
- **Storage**: 50GB SSD (or 100GB recommended)

### Per-Service Allocation

```
┌─────────────────────────────────────────────────┐
│  Backend (Docker - aiml-engine)                 │
│  CPU: 2 cores (limit) / 1 core (reserved)      │
│  RAM: 4GB (limit) / 2GB (reserved)             │
│  Usage: ~70% during forecasting                │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  Frontend (PM2 - 2 instances)                   │
│  CPU: ~0.5 cores per instance                   │
│  RAM: ~500MB per instance (1GB total)          │
│  Usage: Low, spikes during build                │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  Redis (Docker)                                  │
│  CPU: <0.1 cores                                │
│  RAM: ~100-200MB                                │
│  Usage: Minimal                                 │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  Nginx (Docker)                                  │
│  CPU: <0.2 cores                                │
│  RAM: ~50-100MB                                 │
│  Usage: Minimal                                 │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  System (Ubuntu, Docker daemon, etc.)           │
│  CPU: ~0.5 cores                                │
│  RAM: ~1-1.5GB                                  │
└─────────────────────────────────────────────────┘

Total Reserved: ~6GB RAM, ~3.5 vCPUs
Buffer Available: ~2GB RAM, ~0.5-4.5 vCPUs
```

---

## Security Layers

```
┌────────────────────────────────────────────────────────┐
│  Layer 1: Firewall (UFW)                              │
│  • SSH (22), HTTP (80), HTTPS (443) only             │
│  • All other ports blocked                            │
└────────────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────────────┐
│  Layer 2: Nginx Rate Limiting                         │
│  • API: 10 req/sec (burst 20)                        │
│  • Uploads: 2 req/sec (burst 5)                      │
└────────────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────────────┐
│  Layer 3: SSL/TLS (Let's Encrypt)                     │
│  • TLS 1.2 & 1.3 only                                │
│  • Strong cipher suites                               │
│  • HSTS enabled                                       │
└────────────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────────────┐
│  Layer 4: CORS (FastAPI)                              │
│  • Whitelist production domains only                  │
│  • No wildcard origins                                │
└────────────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────────────┐
│  Layer 5: Application Security (Backend)              │
│  • Memory encryption (AES-256-GCM)                    │
│  • Secure logging (PII redaction)                     │
│  • Zero-knowledge proofs                              │
│  • Differential privacy (ε=1.0)                       │
│  • Privacy budget tracking                            │
└────────────────────────────────────────────────────────┘
```

---

## Data Flow & Persistence

### Transient Data (In-Memory)
```
User Upload → Memory (Encrypted)
              ↓
         Data Processing
              ↓
         Result Generation
              ↓
         Response to User
              ↓
         Memory Wiped (Secure)
```

### Persistent Data (Docker Volumes)
```
┌─────────────────────────────────────────────┐
│  uploads_data/                              │
│  └── [Optional: Store uploaded CSVs]        │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  outputs_data/                              │
│  └── [Optional: Store generated reports]    │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  redis_data/                                │
│  └── dump.rdb (Conversational history)      │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  logs_data/                                 │
│  └── application logs (PII-redacted)        │
└─────────────────────────────────────────────┘
```

### Backup Strategy
```
Daily @ 2 AM (Cron Job)
    ↓
Backup Script (/opt/praxifi/backup.sh)
    ↓
┌─────────────────────────────────────────────┐
│  /opt/praxifi/backups/                      │
│  ├── uploads_YYYYMMDD_HHMMSS.tar.gz        │
│  ├── outputs_YYYYMMDD_HHMMSS.tar.gz        │
│  ├── redis_YYYYMMDD_HHMMSS.tar.gz          │
│  ├── logs_YYYYMMDD_HHMMSS.tar.gz           │
│  └── configs_YYYYMMDD_HHMMSS.tar.gz        │
└─────────────────────────────────────────────┘
    ↓
Retention: 7 days (auto-cleanup)
```

---

## Monitoring & Health Checks

### Docker Healthchecks
```
┌─────────────────────────────────────────────┐
│  Redis:                                     │
│  Check: redis-cli ping                     │
│  Interval: 10s, Timeout: 5s, Retries: 3   │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Backend (aiml-engine):                     │
│  Check: curl -f http://localhost:8000/     │
│  Interval: 60s, Timeout: 15s, Retries: 5  │
│  Start Period: 60s                         │
└─────────────────────────────────────────────┘
```

### PM2 Monitoring
```
┌─────────────────────────────────────────────┐
│  Frontend:                                  │
│  • Auto-restart on crash                   │
│  • Memory limit: 1GB (restart if exceeded) │
│  • Min uptime: 10s (prevent crash loop)    │
│  • Max restarts: 10                        │
└─────────────────────────────────────────────┘
```

### Manual Monitoring Commands
```bash
# System resources
htop                    # CPU, Memory
df -h                   # Disk space
free -h                 # Memory usage

# Docker services
docker compose ps       # Container status
docker stats            # Real-time resource usage

# Application logs
docker compose logs -f  # Backend logs
pm2 logs               # Frontend logs

# Network
netstat -tulpn | grep -E '80|443|3000|8000'
```

---

## Deployment Pipeline

```
┌─────────────────────────────────────────────┐
│  1. Local Development                       │
│  • Update domain references                 │
│  • Create environment files                 │
│  • Test builds                              │
│  • Commit & push to GitHub                  │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│  2. VPS Provisioning                        │
│  • Install Docker, Node.js, PM2             │
│  • Configure firewall                       │
│  • Setup fail2ban                           │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│  3. DNS Configuration                       │
│  • Add A records in Namecheap               │
│  • Wait for propagation (15-60 min)         │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│  4. SSL Certificate                         │
│  • certbot certonly --standalone            │
│  • Certificates saved to /etc/letsencrypt   │
│  • Auto-renewal configured (cron)           │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│  5. Application Deployment                  │
│  • Clone repository to /opt/praxifi         │
│  • docker compose build                     │
│  • docker compose up -d                     │
│  • pnpm build && pm2 start                  │
└──────────────────┬──────────────────────────┘
                   ↓
┌─────────────────────────────────────────────┐
│  6. Verification                            │
│  • Test frontend: https://yourdomain.com    │
│  • Test API: https://api.yourdomain.com     │
│  • Upload CSV & generate report             │
│  • Verify progress bar & charts             │
└─────────────────────────────────────────────┘
```

---

## Scaling Considerations

### Vertical Scaling (Current Setup)
- Increase VPS resources (CPU/RAM)
- Suitable for <1000 concurrent users
- Simple, no code changes needed

### Horizontal Scaling (Future)
```
                   ┌── Frontend Instance 1
Load Balancer ────┼── Frontend Instance 2
                   └── Frontend Instance 3
                           ↓
                   ┌── Backend Instance 1
Load Balancer ────┼── Backend Instance 2
                   └── Backend Instance 3
                           ↓
                   Shared Redis Cluster
```

**Required changes**:
- Multiple VPS servers
- External load balancer (nginx/HAProxy)
- Redis cluster for shared state
- Centralized logging (ELK/Loki)
- Shared storage (NFS/S3)

---

**Architecture Version**: v3  
**Last Updated**: November 22, 2025  
**Optimized for**: 4-8 vCPU, 8-16GB RAM VPS
