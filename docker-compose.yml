services:
  # Main AI/ML Engine API Service
  aiml-engine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: praxify-cfo-aiml-engine
    image: praxify-cfo-aiml-engine:latest
    ports:
      - "${API_PORT:-8000}:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - ENV=${ENV:-production}
      - LOG_LEVEL=${LOG_LEVEL:-info}
    volumes:
      # Mount volumes for data persistence
      - uploads_data:/app/uploads  # Persistent storage for uploaded files
      - outputs_data:/app/outputs  # Persistent storage for generated outputs
      - logs_data:/app/logs  # Persistent storage for logs
    restart: unless-stopped
    networks:
      - praxify-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G

  # Optional: Nginx reverse proxy for production
  nginx:
    image: nginx:alpine
    container_name: praxify-cfo-nginx
    ports:
      - "${NGINX_PORT:-80}:80"
      - "${NGINX_SSL_PORT:-443}:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro  # For SSL certificates
    depends_on:
      - aiml-engine
    restart: unless-stopped
    networks:
      - praxify-network
    profiles:
      - with-nginx  # Only start nginx when explicitly requested

volumes:
  uploads_data:
    driver: local
  outputs_data:
    driver: local
  logs_data:
    driver: local

networks:
  praxify-network:
    driver: bridge
